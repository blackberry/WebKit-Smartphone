apt-get install vpnc

cp default.conf /etc/vpnc/
  Edit default.conf changing USERNAME to your RIMNET user name

sudo vpnc
  type in your securID password usa3+'######'

Currently it overides your default route, to have only route a set of ip range add something like your default.conf file:
  Target networks 10.0.0.0/8
